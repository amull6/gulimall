server:
  port: 40000

spring:
  application:
    name: gulimall-cart
  cloud:
    nacos:
      discovery:
        server-addr: 47.92.0.112:8848
    sentinel:
      transport:
        dashboard: localhost:8333
  redis:
    host: 47.92.0.112
    port: 6379

gulimall:
  thread:
    coreSize: 20
    maxSize: 50
    keepAliveTime: 10

management:
  endpoints:
    web:
      exposure:
        include: '*'
feign:
  sentinel:
    enabled: true


